apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: whoami-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/telematiq/tnext_config.git
    targetRevision: HEAD
    path: apps/whoami
  destination:
    server: https://kubernetes.default.svc
    namespace: tnext
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
